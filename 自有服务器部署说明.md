# 蓝星能量星图：Nginx 源码部署说明

本文档适用于 Ubuntu 22.04 / 24.04 或同类 Linux 服务器，不使用 Docker。应用以普通源码方式安装，由 systemd 常驻运行，Nginx 负责域名、HTTPS 和反向代理。

## 一、运行架构

```text
访客浏览器
    |
    | HTTPS 443
    v
Nginx
    |
    | HTTP 127.0.0.1:3000
    v
Node.js + 本地 Worker 运行时
    |
    v
本地 D1 数据目录 /var/lib/blue-planet-energy-map
```

项目基于 vinext、Cloudflare Worker API 与 D1。源码部署仍需使用项目自带的本地 Worker 运行时，但代码、进程和数据库都运行在你的服务器上，不依赖外部 Cloudflare D1。

这套方式适用于个人站点、小团队和中低访问量场景。如果未来需要多服务器、高并发或数据库高可用，建议将数据库迁移至 PostgreSQL，并改造为标准 Node.js 生产运行时。

## 二、服务器要求

最低建议配置：

- 2 核 CPU
- 2 GB 内存
- 20 GB 可用磁盘
- Node.js 22.13 或更高版本
- npm 10 或更高版本
- Nginx
- Git
- OpenSSL

防火墙只需开放 TCP 22、80 和 443。应用的 3000 端口仅监听服务器本机，不应直接暴露到公网。

## 三、安装基础软件

```bash
sudo apt update
sudo apt install -y nginx git curl build-essential openssl
```

请使用服务器发行版认可的方式安装 Node.js 22 LTS。安装后确认版本：

```bash
node --version
npm --version
```

Node.js 版本必须不低于 `22.13.0`。systemd 示例默认 Node 位于 `/usr/bin/node`，可执行以下命令确认：

```bash
command -v node
```

如果输出路径不是 `/usr/bin/node`，需要同步修改 systemd 服务文件中的 `ExecStart`。

## 四、创建独立运行账号和目录

```bash
sudo useradd --system --home /opt/blue-planet-energy-map --shell /usr/sbin/nologin blueplanet
sudo mkdir -p /opt/blue-planet-energy-map/current
sudo mkdir -p /var/lib/blue-planet-energy-map
sudo chown -R blueplanet:blueplanet /opt/blue-planet-energy-map
sudo chown -R blueplanet:blueplanet /var/lib/blue-planet-energy-map
```

独立账号可以限制应用对服务器其他目录的访问。

## 五、上传源码

### 方式一：从 Git 仓库克隆

```bash
sudo -u blueplanet git clone <你的仓库地址> /opt/blue-planet-energy-map/current
```

### 方式二：从本地电脑上传

在本地项目目录执行：

```bash
rsync -av --delete \
  --exclude node_modules \
  --exclude dist \
  --exclude .git \
  --exclude '.env*' \
  ./ 服务器用户@服务器IP:/tmp/blue-planet-energy-map/
```

然后在服务器执行：

```bash
sudo rsync -a /tmp/blue-planet-energy-map/ /opt/blue-planet-energy-map/current/
sudo chown -R blueplanet:blueplanet /opt/blue-planet-energy-map/current
```

## 六、安装依赖并构建

```bash
cd /opt/blue-planet-energy-map/current
sudo -u blueplanet npm ci
sudo -u blueplanet npm run build
```

构建完成后应出现 `dist/` 目录。如果构建失败，不要继续启动服务，先根据终端错误修复。

## 七、生成管理员设置码和会话密钥

```bash
umask 077
SETUP_CODE="BPEM_$(openssl rand -hex 12)"
SESSION_SECRET="$(openssl rand -hex 48)"
sudo sh -c "printf 'ADMIN_SETUP_TOKEN=%s\nADMIN_SESSION_SECRET=%s\n' '$SETUP_CODE' '$SESSION_SECRET' > /etc/blue-planet-energy-map.env"
sudo chmod 600 /etc/blue-planet-energy-map.env
```

查看并保存一次性管理员设置码：

```bash
sudo grep '^ADMIN_SETUP_TOKEN=' /etc/blue-planet-energy-map.env
```

安全要求：

- 不要把 `/etc/blue-planet-energy-map.env` 上传到 Git。
- `ADMIN_SESSION_SECRET` 不需要提供给任何用户。
- 首次管理员设置完成后，设置码不能再次创建管理员。
- 更换 `ADMIN_SESSION_SECRET` 会让所有现有管理员会话失效，但不会删除密码和进度数据。

## 八、安装 systemd 服务

项目中已经提供服务模板：

```bash
sudo cp /opt/blue-planet-energy-map/current/deploy/blue-planet-energy-map.service.example /etc/systemd/system/blue-planet-energy-map.service
sudo systemctl daemon-reload
sudo systemctl enable --now blue-planet-energy-map
```

检查运行状态：

```bash
sudo systemctl status blue-planet-energy-map
sudo journalctl -u blue-planet-energy-map -n 100 --no-pager
```

实时查看日志：

```bash
sudo journalctl -u blue-planet-energy-map -f
```

在服务器本机验证：

```bash
curl -I http://127.0.0.1:3000/
curl http://127.0.0.1:3000/api/admin/session
```

第二个请求应返回类似内容：

```json
{"configured":false,"authenticated":false,"username":null}
```

首次访问接口时，应用会自动创建数据库基础表。

## 九、配置 Nginx

复制项目提供的配置：

```bash
sudo cp /opt/blue-planet-energy-map/current/deploy/nginx.conf.example /etc/nginx/sites-available/blue-planet-energy-map
sudo nano /etc/nginx/sites-available/blue-planet-energy-map
```

将文件中的 `energy.example.com` 替换成你的真实域名，然后启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/blue-planet-energy-map /etc/nginx/sites-enabled/blue-planet-energy-map
sudo nginx -t
sudo systemctl reload nginx
```

反向代理必须保留以下请求头：

```text
Host
X-Forwarded-Host
X-Forwarded-Proto
```

它们用于校验管理员登录及写入请求的来源。

## 十、申请 HTTPS 证书

管理员 Cookie 强制使用 `Secure`，正式环境必须通过 HTTPS 访问。

Ubuntu 可使用 Certbot：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名
```

证书申请完成后测试自动续期：

```bash
sudo certbot renew --dry-run
```

最后访问：

```text
https://你的域名
```

## 十一、首次设置管理员

1. 使用 HTTPS 域名打开网站。
2. 点击右上角“设置管理员”。
3. 管理员账号固定为 `admin`。
4. 输入 `/etc/blue-planet-energy-map.env` 中的 `ADMIN_SETUP_TOKEN`。
5. 设置至少 12 位的新密码。
6. 设置成功后即可新增或删除进度记录。

自有服务器数据库与现有 Sites 数据库相互独立，因此第一次源码部署时需要重新设置管理员密码，原线上站点的新增记录不会自动迁移。

## 十二、发布代码更新

从 Git 更新时执行：

```bash
sudo systemctl stop blue-planet-energy-map
cd /opt/blue-planet-energy-map/current
sudo -u blueplanet git pull --ff-only
sudo -u blueplanet npm ci
sudo -u blueplanet npm run build
sudo systemctl start blue-planet-energy-map
sudo systemctl status blue-planet-energy-map
```

如果使用 rsync 上传，上传完成后同样重新执行 `npm ci`、`npm run build` 并重启服务。

不要删除 `/var/lib/blue-planet-energy-map`，该目录保存进度记录和管理员数据。

## 十三、备份数据

数据库数据位于：

```text
/var/lib/blue-planet-energy-map
```

创建备份：

```bash
sudo systemctl stop blue-planet-energy-map
sudo tar czf /var/backups/blue-planet-energy-map-$(date +%Y%m%d-%H%M%S).tar.gz -C /var/lib blue-planet-energy-map
sudo systemctl start blue-planet-energy-map
```

同时建议备份密钥文件：

```text
/etc/blue-planet-energy-map.env
```

恢复数据前必须停止服务，并先备份当前目录。恢复完成后确认目录所有者：

```bash
sudo chown -R blueplanet:blueplanet /var/lib/blue-planet-energy-map
sudo systemctl start blue-planet-energy-map
```

## 十四、安全检查

- 网站只能通过 HTTPS 对外访问。
- 不要在防火墙开放 3000 端口。
- Nginx 只代理到 `127.0.0.1:3000`。
- `/etc/blue-planet-energy-map.env` 权限保持为 `600`。
- 管理员密码至少 12 位，不与其他服务共用。
- 定期备份 `/var/lib/blue-planet-energy-map`。
- 定期安装系统、Node.js、Nginx 和项目依赖安全更新。
- SSH 建议使用密钥认证并禁用密码登录。

## 十五、常见问题

### systemd 提示找不到 Node.js

执行 `command -v node`，把真实路径填写到 `/etc/systemd/system/blue-planet-energy-map.service` 的 `ExecStart`，然后执行：

```bash
sudo systemctl daemon-reload
sudo systemctl restart blue-planet-energy-map
```

### 网站返回 502 Bad Gateway

先检查应用是否运行：

```bash
sudo systemctl status blue-planet-energy-map
curl -I http://127.0.0.1:3000/
```

### 登录提示“请求来源无效”

检查 Nginx 是否传递 `Host`、`X-Forwarded-Host` 和 `X-Forwarded-Proto`，并确认使用 HTTPS 域名访问。

### 管理员状态暂时不可用

查看服务日志和数据目录权限：

```bash
sudo journalctl -u blue-planet-energy-map -n 200 --no-pager
sudo ls -la /var/lib/blue-planet-energy-map
```

### 更新后服务无法启动

重新检查构建和日志：

```bash
cd /opt/blue-planet-energy-map/current
sudo -u blueplanet npm run build
sudo journalctl -u blue-planet-energy-map -n 200 --no-pager
```
